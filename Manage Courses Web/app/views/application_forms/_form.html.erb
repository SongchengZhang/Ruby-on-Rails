<%= form_with(model: application_form) do |form| %>
  <% if application_form.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(application_form.errors.count, "error") %> prohibited this application_form from being saved:</h2>

      <ul>
        <% application_form.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
  Confirm your email <%= current_user.email %>
  </p>
  <div class="field">
  <%= form.label :userEmail %>
  <%= form.text_field :userEmail, required: true, data: current_user.email %>
</div>

  <div class="field">
  <%= form.label :details %>
  <%= form.text_field :details, required: true %>
</div>

<div class="field">
  <%= form.label :qualifications %>
  <%= form.text_field :qualifications, required: true %>
</div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
